TCP Server?
    -TCP Socket?
        def: ?????
        -Socket Address(o.k)
            def: socket address = IP num + 16-bit port. (192.168.1.255 : 3389)
        -Network Node (ok.)
            def: a device on a network that can send and recieve data.
        -Handle?
    -Port (ok.)
        def: a specific ppoint where data can be sent and recieved. Ports can differentiate
        data. Emails, webpages, etc.
    
    -IP Address:
        Def: A unique identifier for a device on the internet, or local network.


    -Existing Frameworks?
